{
  "name": "lyrics-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Hybrid pipeline tool for creating YouTube lyrics videos",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter remotion-app dev",
    "studio": "pnpm --filter remotion-app studio",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
